from setuptools import setup

setup(name='pythontesting',
      version='0.1',
      description='The best testing ever',
      author='me',
      license='MIT',
      install_requires=[
          'emoji',
      ])